name: Build test
on: [push]

jobs: 
  test-action: 
    runs-on: ubuntu-latest
    steps:
      - uses: brunoom1/FTPDeployAction
        with:
          hostname: ftp.engelux.com.br
          username: engelux
          password: engelux123
